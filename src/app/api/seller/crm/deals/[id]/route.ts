import { NextRequest, NextResponse } from "next/server"; import { resolveSellerCrmContext } from "@/lib/seller-crm/auth";
export async function GET(_:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params; const c=await resolveSellerCrmContext(); if(!c.ok) return NextResponse.json({ok:false,error:c.error,details:c.details},{status:c.status}); const {data,error}=await c.supabase.from("seller_crm_deals").select("*").eq("id",id).eq("seller_id",c.sellerId).maybeSingle(); if(error||!data) return NextResponse.json({ok:false,error:"Not found"},{status:404}); return NextResponse.json({ok:true,data});}
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params; const c=await resolveSellerCrmContext(); if(!c.ok) return NextResponse.json({ok:false,error:c.error,details:c.details},{status:c.status}); const b=await req.json(); const {data:old}=await c.supabase.from("seller_crm_deals").select("deal_stage").eq("id",id).eq("seller_id",c.sellerId).single(); const patch:any={...b,updated_by:c.sellerId}; if(patch.deal_stage==="closed_won"||patch.deal_stage==="closed_lost") patch.closed_at=new Date().toISOString(); const {data,error}=await c.supabase.from("seller_crm_deals").update(patch).eq("id",id).eq("seller_id",c.sellerId).select("*").maybeSingle(); if(error||!data) return NextResponse.json({ok:false,error:error?.message??"Not found"},{status:400}); if(old?.deal_stage!==data.deal_stage){await c.supabase.from("seller_crm_activities").insert({seller_id:c.sellerId,deal_id:data.id,contact_id:data.contact_id,property_id:data.property_id,activity_type:"stage_change",channel:"system",title:"Deal stage changed",old_value:old?.deal_stage,new_value:data.deal_stage});} return NextResponse.json({ok:true,data});}catch(e){console.error("[seller-crm/deal]",e);return NextResponse.json({ok:false,error:"Unexpected error"},{status:500});}}
