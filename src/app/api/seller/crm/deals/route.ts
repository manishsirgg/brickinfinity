import { NextRequest, NextResponse } from "next/server"; import { resolveSellerCrmContext } from "@/lib/seller-crm/auth";
export async function GET(){try{const c=await resolveSellerCrmContext(); if(!c.ok) return NextResponse.json({ok:false,error:c.error},{status:c.status}); const {data,error}=await c.supabase.from("seller_crm_deals").select("*").eq("seller_id",c.sellerId).order("created_at",{ascending:false}); if(error) throw error; return NextResponse.json({ok:true,data:data??[]});}catch(e){console.error("[seller-crm/deal]",e);return NextResponse.json({ok:false,error:"Unexpected error"},{status:500});}}
export async function POST(req:NextRequest){try{const c=await resolveSellerCrmContext(); if(!c.ok) return NextResponse.json({ok:false,error:c.error},{status:c.status}); const b=await req.json(); if(!b.contact_id||!b.title) return NextResponse.json({ok:false,error:"contact_id and title are required"},{status:400}); if(b.property_id){const {data:p}=await c.supabase.from("properties").select("id").eq("id",b.property_id).eq("seller_id",c.sellerId).maybeSingle(); if(!p) return NextResponse.json({ok:false,error:"Invalid property"},{status:400});} const {data,error}=await c.supabase.from("seller_crm_deals").insert({...b,seller_id:c.sellerId,created_by:c.sellerId,updated_by:c.sellerId}).select("*").single(); if(error) return NextResponse.json({ok:false,error:error.message},{status:400}); await c.supabase.from("seller_crm_activities").insert({seller_id:c.sellerId,contact_id:data.contact_id,deal_id:data.id,property_id:data.property_id,activity_type:"deal_created",channel:"system",title:"Deal created",body:data.title}); return NextResponse.json({ok:true,data});}catch(e){console.error("[seller-crm/deal]",e);return NextResponse.json({ok:false,error:"Unexpected error"},{status:500});}}
