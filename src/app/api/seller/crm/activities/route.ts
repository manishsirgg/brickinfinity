import { NextRequest, NextResponse } from "next/server"; import { resolveSellerCrmContext } from "@/lib/seller-crm/auth";
export async function GET(req: NextRequest){try{const c=await resolveSellerCrmContext(); if(!c.ok) return NextResponse.json({ok:false,error:c.error,details:c.details},{status:c.status}); const sp=req.nextUrl.searchParams; let q=c.supabase.from("seller_crm_activities").select("*").eq("seller_id",c.sellerId); for (const k of ["activity_type","channel","contact_id","deal_id","property_id"]) { const v=sp.get(k); if(v) q=q.eq(k,v);} const {data,error}=await q.order("created_at",{ascending:false}).limit(200); if(error) throw error; return NextResponse.json({ok:true,data:data??[]});}catch(e){console.error("[seller-crm/summary]",e);return NextResponse.json({ok:false,error:"Unexpected error"},{status:500});}}
